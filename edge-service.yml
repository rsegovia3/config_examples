zuul:
  ignoredServices: '*'
  routes:
    xactware:
      path: /xactware/**
      stripPrefix: true
      url: http://localhost:2222/test

server:
  port: 8080   # HTTP (Tomcat) port

management:
  security:
     enabled: false
