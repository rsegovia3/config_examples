zuul:
  ignoredServices: '*'
  routes:
    xactware:
      path: /xactware/**
      stripPrefix: true
      url: http://localhost:2222/

server:
  port: 8080   # HTTP (Tomcat) port
security:
  user:
     name: admin
     password: secret
     
management:
  security:
     roles: ACTUATOR
