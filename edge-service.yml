zuul:
  ignoredServices: '*'
  routes:
    xactware:
      path: /test/**
      stripPrefix: true
      url: http://localhost:2222

server:
  port: 8080   # HTTP (Tomcat) port

management:
  security:
     enabled: false
